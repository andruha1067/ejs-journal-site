<%- include('partials/header'); -%>

  <h1>Home</h1>
  <p><%= homeStartingContent %></p>

  <% posts.forEach(function(post) { %>
    <h1><%= post.titleText %></h1>
    <p>
      <%= post.bodyText.substr(0, 100) + " ... "  %>
      <a href="/posts/<%= post.titleText %>">Read More</a>
    </p>
  <% }); %>

<%- include('partials/footer'); -%>
